# specify which being optimized parameters to keep for wandb logging
extra_cfgs:
  skip_proc:
    params:
      motion:
        name: acc_motion_det.AccMotionDet
        params:
          diff_frame_th: [2, 3, 5]

      scale_factor: [1.0]
      block_size_orig: [32, 64]
      block_ratio_th: [0.05, 0.1, 0.15]
      min_roi_ratio: 0.75
