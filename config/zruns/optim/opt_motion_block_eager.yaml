# ! prof. rule
extra_cfgs:
  skip_proc:
    name: motion_only_block_skip_proc_eager # no_skip_proc, rand_skip_proc
    params:
      scale_factor: 0.5
      block_size_orig: 32
      block_ratio_th: 0.05
      motion:
        name: acc_motion_det.AccMotionDet
        params:
          diff_frame_th: 5
          impact_plus_one: 5
          mask_th: 5
          max_val: 15
          decay: 1
      n_chk: [10, 20, 30, 50]
      w_clr: [3, 5, 7, 10]

