# name: metrics_set1
metric_names:
  - accuracy
  - f1_score
  - precision
  - recall (TPR)
  - FPR (False Alarm Rate) # False Positive Rate
  - FPS # Frames Per Second
  # - Skip Rate # Skip Rate = (Number of Skipped Frames) / (Total Number of Safe Frames)
extra_cfgs:
  mode: [per_video, per_frame] # per_video, per_frame
