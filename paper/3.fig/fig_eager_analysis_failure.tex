\begin{figure}[ht]
    \centering
    \includegraphics[width=\linewidth]{3.fig/eager_rs_analyse/eager_timeline_failure.png}
    \caption{A per-frame timeline example illustrating a false-alarm scenario
        under \textsc{Eager} mode. Once the classifier $\mathcal{M}$ produces consecutive
        positive predictions over multiple frames on a static,
        false-alarm-prone scene,
        the system enters \textsc{Eager} mode and persists in it, forcing
        inference on every
    subsequent frame and causing an increase in FAR.}
    \label{fig:eager_analysis_failure}
\end{figure}
