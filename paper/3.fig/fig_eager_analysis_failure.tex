\begin{figure}[ht]
    \centering
    \includegraphics[width=\linewidth]{3.fig/eager_rs_analyse/eager_timeline_failure.png}
    \caption{Per-frame timeline illustrating a false-alarm scenario under
    Eager mode. Once the classifier $\mathcal{M}$ produces
    $W_{\text{fire}}$ consecutive positive predictions on a static
    ambiguous scene, the system enters Eager mode (orange region),
    forcing inference on every subsequent frame and causing a sustained
    increase in FAR until $W_{\text{clr}}$ consecutive negative
    predictions are observed.}
    \label{fig:eager_timeline}
\end{figure}