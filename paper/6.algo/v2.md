let $N_{skip_neg}$ = number of negatives skipped by Skip module.
Each frame frame can be either:
+ group 1: frame would having label FP if processed by DL; size_of_group_1 = a
+ group 2: frame would having label TN if processed by DL; size_of_group_2 = b

$N_{skip_neg} = a + b$;



Then, the skip rate can be expressed as:
_


